# MovieTracker 需求分析文档

> **版本**：v1.0  
> **日期**：2026-06-01  
> **项目类型**：全栈 Web 应用（个人作品 / 毕业设计）  
> **技术栈**：React 19 + Vite 8 + Express 5 + SQLite (better-sqlite3) + ECharts 6

---

## 一、背景与意义

### 1.1 项目背景

豆瓣电影是国内最具影响力的电影评分社区，但其个人观影管理功能薄弱——用户难以系统化管理已看/在看/想看的电影，缺乏跨维度的个人观影数据分析。MovieTracker 即在此背景下启动：既是解决个人观影管理需求的工具，也是涵盖爬虫、后端 API、数据库、前端架构、数据可视化等领域的全栈实践。

### 1.2 项目意义

| 维度 | 意义 |
|------|------|
| **用户价值** | 豆瓣评分参考 + 个人观影进度管理 + 多维度数据分析 |
| **学习价值** | HTTP 爬虫、RESTful API、SQL 优化、React 组件化、ECharts 可视化 |
| **工程价值** | MVC 分层架构、多值字段拆分聚合、暗/亮双主题设计系统 |
| **可复用性** | 架构可扩展到书籍、剧集、游戏等任何媒体追踪系统 |

---

## 二、可行性分析

### 2.1 技术可行性：全部使用成熟开源工具，无阻塞点。

### 2.2 数据可行性：豆瓣 API 可稳定返回 15 标签电影数据，已爬取 2243 部，简介覆盖率 99.9%。

### 2.3 部署可行性：前端纯静态 + 后端 Node 单进程 + SQLite 单文件，1C1G VPS 即可（≈$6/月）。

---

## 三、项目需求分析

### 功能需求（6 大模块）

- **FR-1 电影数据管理**：豆瓣爬虫 + 手动增删改 + douban_id 去重 + 增量更新
- **FR-2 搜索与排名**：名称/导演模糊搜索 + 四维排名（总/同年/同类型/同导演）
- **FR-3 用户操作**：观影进度（想看/在看/已看）+ 1-10 星评分 + 影评 + 批量操作
- **FR-4 自定义清单**：CRUD + 批量添加 + 清单内进度（双向同步）
- **FR-5 数据分析**：全站 6 图 + 个人 9 图 + 概览卡片 + 多值拆分聚合
- **FR-6 UI/UX**：亮色主题 + 毛玻璃导航 + 响应式 + 分页加载

### 非功能需求

性能（<200ms）、安全（参数化查询）、数据安全（爬虫不覆盖用户数据）、可用性、可维护性。

---

## 四、项目总体设计

### 系统架构

```
浏览器 (React 19) → Vite Proxy → Express 5 (:3001) → SQLite (data.db)
                                                 ├── items.js (CRUD+搜索)
                                                 ├── analytics.js (分析)
                                                 └── lists.js (清单)
离线工具：scraper.js (豆瓣爬虫) / scrape-summaries-only.js (简介爬虫)
```

### 数据库

核心表 `items`（20 字段）+ 清单 `lists` / `list_items`（双向同步）。

### API

25+ 端点，RESTful 资源命名，查询参数做筛选/排序/分页。

---

## 五、模块设计

- **后端 5 模块**：db (初始化+迁移+索引) / items (CRUD+搜索+排名) / analytics (拆分聚合) / lists (双向同步) / scraper (限流+熔断+UPSERT)
- **前端 8 页面 + 3 共享组件**：List / Search / MovieDetail / AddEdit / Analytics / Profile / Lists / ListDetail；Chart / Poster / StarRating

---

## 六、风险预估

| 风险 | 影响 | 应对 |
|------|------|------|
| 豆瓣 API 变更/关闭 | 高 | 已有 2243 部数据基础；爬虫层独立可替换数据源 |
| IP 限流/封禁 | 中 | 限流 1s + 熔断 30 次失败自动停止 |
| 无用户认证 | 高 | 上线前接入 JWT（方案已设计） |
| data.db 入 Git 泄露隐私 | 中 | 上线前 .gitignore 忽略 |
| Express 5 兼容性 | 低 | keepAliveTimeout=0 workaround |

---

## 七、迭代规划

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 MVP | 爬虫 + CRUD + 基础列表 | ✅ |
| Phase 2 增强 | 搜索排名 + 进度/评分 + 全站分析 | ✅ |
| Phase 3 清单 | 自定义清单 + 双向同步 + 批量 | ✅ |
| Phase 4 个人 | 个人分析 + 多图表 + 详情页 | ✅ |
| Phase 5 上线 | 认证 + 安全 + 部署 | ⏳ |

---

> **文档维护者**：Agent · **最后更新**：2026-06-01
